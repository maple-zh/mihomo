<h3><div align="center">mihomo 镜像 (Zashboard 版)</div></h3>
---
<div align="center">
  <img src="https://img.shields.io/github/last-commit/<你的GitHub用户名>/mihomo" alt="最后提交">
  <img src="https://img.shields.io/github/actions/workflow/status/<你的GitHub用户名>/mihomo/docker-build.yml" alt="构建状态">
  <a href="https://github.com/<你的GitHub用户名>/mihomo/blob/main/License">
    <img src="https://img.shields.io/badge/License-MIT-yellow?style=flat-square" alt="MIT许可证">
  </a>
  <a href="https://github.com/<你的GitHub用户名>/mihomo/pkgs/container/mihomo">
    <img src="https://img.shields.io/badge/GHCR.io-Package-blue?logo=github" alt="GHCR Package">
  </a>
</div>

mihomo 是一个基于 Clash 核心的代理工具镜像，集成了 **Zashboard** 管理面板，提供强大的代理功能和直观的 Web 管理界面。

**构建信息**
- mihomo 核心版本: `{{MI_VERSION}}`
- Zashboard 面板版本: `{{ZASHBOARD_VERSION}}`

### 拉取镜像
```bash
# GitHub Container Registry
docker pull ghcr.io/<你的GitHub用户名>/mihomo:{{MI_VERSION}}
# 启动容器
docker run -d --name mihomo -p 7890:7890 -p 8080:8080 ghcr.io/<你的GitHub用户名>/mihomo:{{MI_VERSION}}
```

### docker-compose.yml 配置文件
```yaml
version: '3.8'
services:
  mihomo:
    container_name: mihomo
    image: ghcr.io/<你的GitHub用户名>/mihomo:latest
    restart: always
    environment:
      # 核心配置
      - TZ=Asia/Shanghai
      - LOG_LEVEL=silent
      - CLASH_SECRET=
      - SUBSCRIBE_URL=your_subscribe_url
      - SUBSCRIBE_NAME=your_subscribe_name
    ports:
      - "7890:7890"  # HTTP代理
      - "7891:7891"  # SOCKS5代理
      - "7892:7892"  # 混合代理
      - "7893:7893"  # TPROXY
      - "7894:7894"  # REDIR
      - "9090:9090"  # Clash控制API
      - "8080:8080"  # Zashboard Web UI
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9090"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - ./clash-config:/root/.config/mihomo  # Clash配置文件目录
      - ./zashboard-config:/config/caddy     # Zashboard/Caddy配置目录
      # - /etc/timezone:/etc/timezone:ro       # 共享主机时区
      # - /etc/localtime:/etc/localtime:ro
    # 如需启用TUN模式，取消以下注释
    # cap_add:
    #   - NET_ADMIN
    # devices:
    #   - /dev/net/tun:/dev/net/tun
    networks:
      - clash-net
networks:
  clash-net:
    driver: bridge
```

>
> 仓库地址：https://github.com/<你的GitHub用户名>/mihomo
>
> 项目源码：[mihomo](https://github.com/MetaCubeX/mihomo) | [Zashboard](https://github.com/Zephyruso/zashboard)
>
> 协议：[MIT](https://github.com/<你的GitHub用户名>/mihomo/blob/main/License)
